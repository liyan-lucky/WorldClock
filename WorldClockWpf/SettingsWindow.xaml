<Window x:Class="WorldClockWpf.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="设置"
        Width="300"
        Height="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="ToolWindow">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="添加地点" FontWeight="SemiBold" Margin="0,0,0,6" />
        <TextBox Grid.Row="1" x:Name="SearchBox" TextChanged="SearchBox_TextChanged" />
        <ListBox Grid.Row="2" x:Name="CandidateList" Height="112" Margin="0,8,0,10" />

        <CheckBox Grid.Row="3" x:Name="TopMostCheckBox" Content="窗口置顶" Margin="0,0,0,8" />
        <CheckBox Grid.Row="4" x:Name="ShowSecondsCheckBox" Content="显示秒数" Margin="0,0,0,10" />

        <Grid Grid.Row="5">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="透明度" Margin="0,0,0,6" />
            <ComboBox Grid.Row="1" x:Name="OpacityComboBox" Margin="0,0,0,10" />
            <TextBlock Grid.Row="2" Text="主题" Margin="0,0,0,6" />
            <ComboBox Grid.Row="3" x:Name="ThemeComboBox" />
        </Grid>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Width="72" Margin="0,0,8,0" Content="取消" Click="CancelButton_Click" />
            <Button Width="72" Content="应用" Click="ApplyButton_Click" />
        </StackPanel>
    </Grid>
</Window>
